trigger: 
- main

pool: 
   vmImage: windows

jobs:
- deployment: VMDeploy
  displayName: Deploy to VM
  environment: 
   name: ranorex
   resourceType: VirtualMachine
  strategy:
     runOnce:
        deploy:   
          steps:
            - script: echo "Hello world"