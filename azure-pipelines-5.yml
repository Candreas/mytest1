# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'build'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:DesktopBuildPackageLocation="$(build.artifactStagingDirectory)\WebApp.zip" /p:DeployIisAppPath="Default Web Site"'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'
    clean: true
- task: CmdLine@2
  inputs:
    script: |
      echo $(buildPlatform)
      echo $(buildConfiguration)
      echo $(Build.StagingDirectory)
      echo $(System.DefaultWorkingDirectory)
- task: VSTest@2
  inputs:
    testSelector: 'testAssemblies'
    testAssemblyVer2: |
      **\*test*.dll
      !**\*TestAdapter.dll
      !**\obj\**
    searchFolder: '$(System.DefaultWorkingDirectory)'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'
      
      echo Hello world
- task: VSTest@2
  inputs:
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      # Write your PowerShell commands here.
      #Get-ChildItem -Path c:\agent_work\1\s
      Write-Host "Hello World"
      #Write-Host $(Build.StagingDirectory)/mytest1
      Write-Host "Hello World"
      Write-Host "Hello World"
      Get-ChildItem -Path C:\WindowsAzure\Logs
      Get-ChildItem -Path $(Build.StagingDirectory)
      #Write-Host $(env)
      # Predefined Variables
      #$adSdkUrl = $(Build.SourcesDirectory)\MyTest1.sln
      #$adSdkPath = Join-Path $env:TEMP "RanorexRemoteAgent-7.3.0.msi"
      #Write-Host $(adSdkUrl)
